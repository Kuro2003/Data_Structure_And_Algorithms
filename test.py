# Initialize a string
str = "Learn"

# Initialize a list
list = ["Share", "IT"]

# Use a for loop to iterate through the elements in the list
for l in list:
    str = str + " " + l # Update str

print(str)